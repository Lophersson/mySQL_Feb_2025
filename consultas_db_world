SELECT 
    country.Name AS Country,
    COUNT(city.ID) AS CityCount
FROM 
    country
LEFT JOIN 
    city ON country.Code = city.CountryCode
GROUP BY 
    country.Name
ORDER BY 
    CityCount DESC;
---
  
